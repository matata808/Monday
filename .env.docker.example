APP_SECRET=replace-with-a-long-random-local-secret

POSTGRES_DB=dashboard
POSTGRES_USER=dashboard
POSTGRES_PASSWORD=replace-with-a-local-dev-password

# Optional Gmail OAuth values. The redirect URI for this compose setup is:
# http://127.0.0.1:8787/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional ZFN IMAP config.
ZFN_IMAP_HOST=imap.uni-bremen.de
ZFN_IMAP_PORT=993

# Weather integration. Open-Meteo does not require an API key.
WEATHER_API_PROVIDER=open-meteo
WEATHER_API_KEY=
WEATHER_LOCATION=Berlin,DE
